k8sServiceHost: "{{ ansible_host }}"
k8sServicePort: 6443

kubeProxyReplacement: true

hostServices:
  enabled: true

prometheus:
  enabled: true
  serviceMonitor:
    enabled: {{ (servicemonitor_crd.rc == 0) | bool | lower }}

operator:
  prometheus:
    enabled: true
    serviceMonitor:
      enabled: {{ (servicemonitor_crd.rc == 0) | bool | lower }}

hubble:
  enabled: true
  relay:
    enabled: true
  ui:
    enabled: true
  metrics:
    enabled:
      - dns
      - drop
      - tcp
      - flow
      - icmp
      - http
    serviceMonitor:
      enabled: {{ (servicemonitor_crd.rc == 0) | bool | lower }}
